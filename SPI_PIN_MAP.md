# APEX SPI Pin Map

Sourced directly from `APEX_H7/APEX_H7.ioc`, `APEX_H7/Core/Inc/main.h`,
`APEX_H7/Core/Src/main.c`, and `APEX_H7/Core/Src/stm32h7xx_hal_msp.c`.
Read from actual assigned values — not inferred from CubeMX Arduino defaults.

## Pin Table

| Signal Role | SPI Peripheral | STM32 GPIO | #define / Label |
|---|---|---|---|
| SCK | SPI1 | PA5 | *(no CS-style label; `SPI1_SCK`, AF5)* |
| MISO | SPI1 | PA6 | *(no label; `SPI1_MISO`, AF5)* |
| MOSI | SPI1 | PB5 | *(no label; `SPI1_MOSI`, AF5)* |
| CS (IMU0) | SPI1 | PA4 | `CS_IMU0_Pin` / `CS_IMU0_GPIO_Port` (GPIOA) |
| CS (IMU1) | SPI1 | PC7 | `CS_IMU1_Pin` / `CS_IMU1_GPIO_Port` (GPIOC) |
| CS (IMU2) | SPI1 | PC6 | `CS_IMU2_Pin` / `CS_IMU2_GPIO_Port` (GPIOC) |
| CS (IMU3) | SPI1 | PB15 | `CS_IMU3_Pin` / `CS_IMU3_GPIO_Port` (GPIOB) |
| CS (IMU4) | SPI1 | PF3 | `CS_IMU4_Pin` / `CS_IMU4_GPIO_Port` (GPIOF) |
| SCK | SPI3 | PC10 | *(no label; `SPI3_SCK`, AF7)* |
| MISO | SPI3 | PC11 | *(no label; `SPI3_MISO`, AF7)* |
| MOSI | SPI3 | PB2 | *(no label; `SPI3_MOSI`, AF7)* |
| CS (SD card) | SPI3 | PD2 | `CS_SDCard0_Pin` / `CS_SDCard0_GPIO_Port` (GPIOD) |

### Source lines (`APEX_H7.ioc`)

```
129:PA5.Signal=SPI1_SCK
131:PA6.Signal=SPI1_MISO
142:PB5.Signal=SPI1_MOSI
144:PC10.Signal=SPI3_SCK
146:PC11.Signal=SPI3_MISO
139:PB2.Signal=SPI3_MOSI
123:PA4.GPIO_Label=CS_IMU0
158:PC7.GPIO_Label=CS_IMU1
152:PC6.GPIO_Label=CS_IMU2
133:PB15.GPIO_Label=CS_IMU3
176:PF3.GPIO_Label=CS_IMU4
164:PD2.GPIO_Label=CS_SDCard0
```

### `main.h`

```c
#define CS_IMU4_Pin GPIO_PIN_3
#define CS_IMU4_GPIO_Port GPIOF
#define CS_IMU0_Pin GPIO_PIN_4
#define CS_IMU0_GPIO_Port GPIOA
#define CS_IMU3_Pin GPIO_PIN_15
#define CS_IMU3_GPIO_Port GPIOB
#define CS_IMU2_Pin GPIO_PIN_6
#define CS_IMU2_GPIO_Port GPIOC
#define CS_IMU1_Pin GPIO_PIN_7
#define CS_IMU1_GPIO_Port GPIOC
#define CS_SDCard0_Pin GPIO_PIN_2
#define CS_SDCard0_GPIO_Port GPIOD
```

### `stm32h7xx_hal_msp.c` (SPI AF pin comments, generated by CubeMX)

```
/**SPI1 GPIO Configuration
PA5     ------> SPI1_SCK
PA6     ------> SPI1_MISO
PB5     ------> SPI1_MOSI
...
/**SPI3 GPIO Configuration
PB2     ------> SPI3_MOSI
PC10     ------> SPI3_SCK
PC11     ------> SPI3_MISO
```

## Answers

**(a) Same bus or different buses?**

Different buses. All 5 IMUs are on **SPI1** (PA5/PA6/PB5). The SD card is on
**SPI3** (PC10/PC11/PB2) — a completely separate peripheral instance.
Note: the actual SD MOSI pin is **PB2**, not PC12 as the placeholder note in
`CLAUDE.md` ("verify against your CubeMX") suggested — that doc should be
updated to match.

**(b) Every CS/NSS GPIO and what it selects**

| GPIO | Selects |
|---|---|
| PA4 (`CS_IMU0`) | IMU 0 (SPI1) |
| PC7 (`CS_IMU1`) | IMU 1 (SPI1) |
| PC6 (`CS_IMU2`) | IMU 2 (SPI1) |
| PB15 (`CS_IMU3`) | IMU 3 (SPI1) |
| PF3 (`CS_IMU4`) | IMU 4 (SPI1) |
| PD2 (`CS_SDCard0`) | SD card (SPI3) |

All six are software-driven GPIO outputs (`GPIO_MODE_OUTPUT_PP`,
`main.c:435-497`), not hardware NSS — consistent with
`hspi1.Init.NSS = SPI_NSS_SOFT` / `hspi3.Init.NSS = SPI_NSS_SOFT`
(`main.c:312,359`) and `NSSPMode = SPI_NSS_PULSE_DISABLE` on both (per the H7
erratum rule in `CLAUDE.md`). All six CS pins are also initialized
`GPIO_PIN_SET` (idle-high) at boot (`main.c:450-462`), matching the "LSM6DSO
CS must be HIGH at power-on" invariant. Both SPI1 and SPI3 use
`CLKPolarity = SPI_POLARITY_LOW` / `CLKPhase = SPI_PHASE_1EDGE` (Mode 0),
matching the CLAUDE.md requirement.
